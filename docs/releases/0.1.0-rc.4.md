# threadBridge 0.1.0-rc.4

Release candidate built from the latest Telegram workspace cleanup code.

Highlights:

- cleans stale Telegram workspace topics from the local control and management surfaces
- preserves the release runtime data root under ~/Library/Application Support/threadBridge
- keeps the bundled runtime support path separate from repo-local development state

Known limitations:

- Homebrew tap publication is not included in this RC
- release automation is currently local-operator driven
